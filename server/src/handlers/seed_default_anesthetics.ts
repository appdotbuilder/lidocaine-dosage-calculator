
import { type Anesthetic } from '../schema';

export async function seedDefaultAnesthetics(): Promise<Anesthetic[]> {
  // This is a placeholder declaration! Real code should be implemented here.
  // The goal of this handler is to populate the database with default
  // anesthetic options including:
  // - Lidocaine (max 4.5 mg/kg, common concentrations: 10, 20 mg/mL)
  // - Other common local anesthetics used in circumcision procedures
  // This ensures the application has proper medical data on first use.
  return [];
}
